{
  "name": "tourism-platform-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd \"Travel AI Pro\" && npm run dev",
    "dev:backend": "cd Backend && npm run dev",
    "install:all": "npm install && cd \"Travel AI Pro\" && npm install && cd ../Backend && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
